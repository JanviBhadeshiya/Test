<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Birthday Question</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      height: 100vh;
      overflow: hidden;
      background: #ffc0cb;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    /* Floating Hearts */
    .heart {
      position: absolute;
      color: rgba(255, 0, 0, 0.25);
      font-size: 30px;
      animation: float 10s linear infinite;
    }

    @keyframes float {
      0% {
        transform: translateY(100vh) scale(1);
      }
      100% {
        transform: translateY(-120vh) scale(1.5);
      }
    }

    .container {
      text-align: center;
      background: rgba(255, 255, 255, 0.25);
      padding: 40px;
      border-radius: 25px;
      backdrop-filter: blur(8px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      z-index: 2;
    }

    h1 {
      color: #c2185b;
      margin-bottom: 20px;
      font-size: 2rem;
    }

    .cute-img {
      width: 220px;
      border-radius: 20px;
      margin-bottom: 25px;
    }

    .buttons {
      display: flex;
      gap: 20px;
      justify-content: center;
      align-items: center;
      margin-top: 20px;
    }

    button {
      border: none;
      padding: 15px 35px;
      font-size: 18px;
      border-radius: 50px;
      cursor: pointer;
      transition: all 0.3s ease;
      color: white;
      font-weight: bold;
    }

    #yesBtn {
      background: #28a745;
      transform: scale(1);
    }

    #noBtn {
      background: #dc3545;
      transform: scale(1);
    }

    #message {
      margin-top: 25px;
      font-size: 24px;
      color: #c2185b;
      font-weight: bold;
    }
  </style>
</head>

<body>

  <!-- Floating Hearts -->
  <div class="heart" style="left:10%; animation-duration: 9s;">❤</div>
  <div class="heart" style="left:25%; animation-duration: 12s;">❤</div>
  <div class="heart" style="left:40%; animation-duration: 8s;">❤</div>
  <div class="heart" style="left:55%; animation-duration: 14s;">❤</div>
  <div class="heart" style="left:70%; animation-duration: 10s;">❤</div>
  <div class="heart" style="left:85%; animation-duration: 13s;">❤</div>

  <div class="container">
    <h1>What do you want for your birthday, Husband? 🎮</h1>

    <!-- Cute Cat GIF -->
    <img 
      class="cute-img"
      src="https://media.giphy.com/media/mlvseq9yvZhba/giphy.gif"
      alt="Cute Cat"
    />

    <div class="buttons">
      <button id="yesBtn">PS4 🎮</button>
      <button id="noBtn">Nothing</button>
    </div>

    <div id="message"></div>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");
    const message = document.getElementById("message");

    const texts = [
      "Are you sure?",
      "Pretty Please 🥺",
      "It will make me happy ❤️"
    ];

    let clickCount = 0;
    let yesScale = 1;
    let noScale = 1;

    noBtn.addEventListener("click", () => {

      if (clickCount < texts.length) {
        noBtn.innerText = texts[clickCount];

        noScale -= 0.2;
        yesScale += 0.25;

        noBtn.style.transform = `scale(${noScale})`;
        yesBtn.style.transform = `scale(${yesScale})`;

        clickCount++;
      } else {
        noBtn.style.display = "none";
        message.innerHTML = "YAYYYY!! PS4 IT IS 🎉🎮❤️";
      }
    });

    yesBtn.addEventListener("click", () => {
      message.innerHTML = "BEST HUSBAND EVER ❤️🎮";
    });
  </script>

</body>
</html>